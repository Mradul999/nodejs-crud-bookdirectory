# Node.js CRUD app for a book directtory with GET,POST,PUT and DELETE

## Instructions

- clone the repo-git clone
- install dependencies - npm install
- set env variables - DB_URL, PORT
- run the project - npm run dev
